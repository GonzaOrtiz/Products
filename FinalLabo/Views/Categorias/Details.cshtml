@model FinalLabo.Models.Categoria

@{
    ViewData["Title"] = "Details";
}

<h3>Detalles</h3>

<div>
    <h4>Categoria</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Descripcion)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Descripcion)
        </dd>
    </dl>
</div>
<div>

    <a asp-action="Edit" class="btn btn-primary shadow-sm" asp-route-id="@Model.Id">Editar</a>
    <a class="btn btn-danger shadow-sm" asp-action="Index">Regresar</a>
</div>
